# 🎯 เริ่มต้นใช้งานแบบง่าย - สำหรับผู้ที่ WordPress อยู่บน Hosting

## 📖 สถานการณ์ของคุณ

✅ **WordPress**: อยู่บน Hosting แล้ว (เช่น Hostinger, Bluehost, SiteGround)  
❌ **Middleware API**: ยังอยู่ที่ localhost เท่านั้น

**ปัญหา:** WordPress บน hosting ไม่สามารถเข้าถึง localhost ของคุณได้

## 🚀 แก้ปัญหา: Deploy Middleware ขึ้น Server

---

## วิธีที่ 1: ใช้ Railway.app (แนะนำ - ฟรีและง่ายที่สุด)

### ⏱️ ใช้เวลา: 10-15 นาที

### ขั้นตอน:

#### 1. เตรียม Project สำหรับ GitHub

```bash
cd /Users/devbo/Desktop/sportmonks-middleware

# รันสคริปต์เตรียม
./prepare-github.sh
```

#### 2. สร้าง GitHub Repository

1. ไปที่ https://github.com/new
2. ตั้งชื่อ: `sportmonks-middleware`
3. เลือก **Public** (ถ้าใช้ฟรี) หรือ **Private** (ถ้ามี GitHub Pro)
4. **ไม่ต้อง** เลือก Initialize repository
5. คลิก "Create repository"

#### 3. Push Code ขึ้น GitHub

```bash
# เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME ด้วยชื่อผู้ใช้จริง)
git remote add origin https://github.com/YOUR_USERNAME/sportmonks-middleware.git

# Push code
git branch -M main
git push -u origin main
```

#### 4. Deploy บน Railway

1. **สมัครสมาชิก Railway**
   - ไปที่ https://railway.app
   - คลิก "Login" → เลือก "Login with GitHub"
   - อนุญาตให้ Railway เข้าถึง GitHub

2. **สร้าง Project ใหม่**
   - คลิก "New Project"
   - เลือก "Deploy from GitHub repo"
   - เลือก `sportmonks-middleware` repository

3. **เพิ่ม Redis**
   - ใน Project คลิก "+ New"
   - เลือก "Database" → "Add Redis"
   - รอสักครู่จนเห็น Redis service พร้อมใช้งาน

4. **ตั้งค่า Environment Variables**
   - คลิกที่ Service ชื่อ `sportmonks-middleware` (ไม่ใช่ Redis)
   - ไปที่แท็บ "Variables"
   - คลิก "New Variable" เพื่อเพิ่มตัวแปรต่อไปนี้:

   ```
   NODE_ENV=production
   SPORTMONKS_API_KEY=ใส่_API_Key_จริงที่นี่
   SPORTMONKS_BASE_URL=https://api.sportmonks.com/v3/football
   ALLOWED_ORIGINS=https://yourwordpress.com
   ```

   **สำคัญ:** 
   - แทน `ใส่_API_Key_จริงที่นี่` ด้วย API Key จริงจาก SportMonks
   - แทน `https://yourwordpress.com` ด้วย URL WordPress จริงของคุณ

   **สำหรับ Redis:** Railway จะเชื่อมต่ออัตโนมัติ ไม่ต้องตั้งค่า REDIS_HOST

5. **รอ Deploy เสร็จ**
   - Railway จะ build และ deploy อัตโนมัติ
   - รอประมาณ 2-3 นาที
   - เมื่อเสร็จจะเห็นสถานะเป็น "Active"

6. **ดู URL ของ API**
   - คลิกที่ Service `sportmonks-middleware`
   - ไปที่แท็บ "Settings"
   - ดูที่ "Domains"
   - คลิก "Generate Domain" (ถ้ายังไม่มี)
   - คุณจะได้ URL เช่น: `https://sportmonks-middleware-production.up.railway.app`

7. **ทดสอบ API**
   
   เปิด Browser ไปที่:
   ```
   https://sportmonks-middleware-production.up.railway.app/api/health
   ```
   
   ถ้าเห็น `{"success": true, "message": "SportMonks Middleware API is running"}` แสดงว่าสำเร็จ! ✅

---

#### 5. ติดตั้ง WordPress Plugin

1. **Upload Plugin**
   - ไปที่ WordPress Admin
   - เลือก Plugins → Add New → Upload Plugin
   - เลือกไฟล์ `sportmonks-middleware.zip` ที่อยู่ในโฟลเดอร์ `wordpress-plugin/`
   - คลิก "Install Now" → "Activate Plugin"

2. **ตั้งค่า Plugin**
   - ไปที่ Settings → SportMonks API
   - ใส่ **API Base URL**:
     ```
     https://sportmonks-middleware-production.up.railway.app/api
     ```
   - ตั้งค่า **Cache Duration**: `300` (5 นาที)
   - คลิก "Save Changes"

---

#### 6. ทดสอบ Shortcode

1. สร้าง Post/Page ใหม่
2. ใส่ Shortcode:
   ```
   [live_scores]
   
   [league_standings league_id="8"]
   ```
3. คลิก "Preview"
4. ถ้าเห็นข้อมูลแสดงว่าสำเร็จ! 🎉

---

## วิธีที่ 2: ใช้ ngrok สำหรับทดสอบเร็วๆ (ไม่แนะนำสำหรับใช้งานจริง)

### เหมาะสำหรับ: ทดสอบเท่านั้น ไม่เสถียร

```bash
# 1. ติดตั้ง ngrok
brew install ngrok

# 2. สมัครสมาชิก (ฟรี)
# ไปที่ https://ngrok.com → Sign up
# คัดลอก Authtoken

# 3. ตั้งค่า authtoken
ngrok config add-authtoken YOUR_AUTHTOKEN

# 4. รัน Middleware (terminal แรก)
cd /Users/devbo/Desktop/sportmonks-middleware
npm start

# 5. รัน ngrok (terminal ใหม่)
ngrok http 3000
```

**คุณจะได้ URL เช่น:**
```
https://abc123.ngrok-free.app
```

**ตั้งค่าใน WordPress:**
```
API Base URL: https://abc123.ngrok-free.app/api
```

**⚠️ ข้อเสีย:**
- URL เปลี่ยนทุกครั้งที่รัน ngrok ใหม่
- ต้องเปิด Terminal ไว้ตลอดเวลา
- ไม่เสถียร ไม่เหมาะสำหรับ Production

---

## 🆘 แก้ไขปัญหาที่พบบ่อย

### ปัญหา: CORS Error

```
Access to fetch has been blocked by CORS policy
```

**วิธีแก้:**
1. ไปที่ Railway → Service → Variables
2. ตรวจสอบว่า `ALLOWED_ORIGINS` มี URL WordPress ที่ถูกต้อง
3. ถ้า WordPress มีทั้ง `www` และไม่มี ให้ใส่ทั้ง 2:
   ```
   ALLOWED_ORIGINS=https://yoursite.com,https://www.yoursite.com
   ```
4. Redeploy service

---

### ปัญหา: WordPress แสดง "ไม่สามารถโหลดข้อมูลได้"

**วิธีแก้:**
1. ตรวจสอบว่า API รันอยู่: เปิด Browser ไปที่ `YOUR_API_URL/api/health`
2. ตรวจสอบ URL ใน WordPress Settings (Settings → SportMonks API)
3. ตรวจสอบว่าใส่ `/api` ท้าย URL แล้ว

---

### ปัญหา: Railway แสดง Build Failed

**วิธีแก้:**
1. ตรวจสอบว่า push `package.json` ขึ้น GitHub แล้ว
2. ตรวจสอบ logs ใน Railway → Deployments → คลิกที่ deployment ล่าสุด
3. ถ้าเจอ error ให้ copy มาถามได้เลย

---

## 📊 ตรวจสอบว่า Deploy สำเร็จ

### ✅ Checklist:

- [ ] API Health Check ผ่าน: `https://your-api.com/api/health`
- [ ] ทดสอบดึง Live Scores: `https://your-api.com/api/livescores`
- [ ] WordPress Plugin ถูก activate
- [ ] ตั้งค่า API URL ใน WordPress Settings แล้ว
- [ ] Shortcode `[live_scores]` แสดงข้อมูลได้
- [ ] ไม่มี CORS error ใน Console (F12)

---

## 💰 ค่าใช้จ่าย

### Railway (แนะนำ)
- **Free Tier**: $5 credit/เดือน (เพียงพอสำหรับทดสอบ)
- **Hobby**: $5/เดือน (unlimited usage)

### ngrok
- **Free**: URL เปลี่ยนทุกครั้ง
- **Paid**: $8/เดือน (fixed URL)

---

## 🎓 สรุป

1. **Deploy Middleware** บน Railway (10-15 นาที)
2. **ติดตั้ง Plugin** ใน WordPress (5 นาที)
3. **ตั้งค่า API URL** ใน WordPress Settings
4. **ทดสอบ Shortcode** ในหน้า WordPress

**รวมเวลา:** ประมาณ 20-30 นาที

---

## 📞 ต้องการความช่วยเหลือ?

หากติดขั้นตอนไหน สามารถถามได้เลยครับ! 😊
